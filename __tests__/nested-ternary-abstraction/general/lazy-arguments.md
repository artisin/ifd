__Description__: Should be able to lazy eval an argument based idf within a idf instance through an Array with `ifd` as the first arg

__Syntax__

```
const foo = ifd([[ifd, <arg>, <arg>, <arg>]], [<if>, <return>]);
```

__Notes__

+ Original options applied unless an option Object is passed